# ---------------------------------------------------------------------------
# Install:  python -m venv .venv && .\.venv\Scripts\pip install -r requirements.txt
# Everything here is open-source and runs locally on CPU. No API keys.
# ---------------------------------------------------------------------------

# --- Structure handling ---
biopython>=1.83      # parse PDB/mmCIF, geometry, neighbor search
numpy>=1.26
scipy>=1.11          # KD-trees, spatial math
pandas>=2.1          # the per-residue feature table
requests>=2.31       # download structures from RCSB / AlphaFold DB
truststore>=0.9      # use the OS cert store (fixes SSL on proxied networks)

# --- Model ---
scikit-learn>=1.4    # cross-validation, metrics, preprocessing
xgboost>=2.0         # the accepted model

# --- Visualization ---
matplotlib>=3.8

# --- Dev ---
pytest>=8.0

# ---------------------------------------------------------------------------
# Optional: faster SASA backend. Needs a C compiler; the pipeline falls back to
# Biopython's Shrake-Rupley automatically if it is absent, so skip it on Windows
# unless MSVC build tools are already installed.
# ---------------------------------------------------------------------------
# freesasa>=2.2

# ---------------------------------------------------------------------------
# Optional: only needed to re-run the rejected approaches in experiments/.
# ---------------------------------------------------------------------------
# torch>=2.2
# torch-geometric>=2.5
# transformers>=4.40
# pdb2pqr>=3.6
